# Recent UI changes

src/ActionButton.jsx was moved to src/Button.jsx and its ActionButton export was renamed Button. The primary and destructive variants remain the same component identities.

In src/tokens.css, --control-radius changed from 4px to 8px. The dark-theme --focus-ring changed from #a8c7fa to #d3e3fd. No new design baseline was approved.
