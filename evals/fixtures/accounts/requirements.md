# Account service

Users own documents. Signed-in users may access only their own documents.
The application currently has no account deletion or background purge process.
There is no agreed retention policy or recovery duration.
